# 0-intro to umask
---
## تمام چیزایی که درمورد umask باید بدونی
1. مفهوم `umask`
2. کاربرد `umask`
3. مقدار پیش‌فرض `umask`
4. نحوه مشاهده `umask`
5. نحوه تغییر `umask`
6. فرمت عددی `umask` مثل `022`، `002`، `077`
7. رابطه `umask` با Permissionهای پیش‌فرض
8. ا-Permission پیش‌فرض فایل‌ها: `666`
9. ا-Permission پیش‌فرض دایرکتوری‌ها: `777`
10. نحوه محاسبه Permission نهایی با `umask`
11. تفاوت `umask` برای فایل و دایرکتوری
12. دستور `umask`
13. دستور `umask -S`
14. تفاوت Symbolic و Numeric در `umask`
15. تغییر موقت `umask`
16. تنظیم دائمی `umask`
17. ا-`umask` در `.bashrc`
18. ا-`umask` در `/etc/profile`
19. تأثیر `umask` روی فایل‌های جدید
20. تأثیر `umask` روی دایرکتوری‌های جدید
21. مثال‌های محاسبه `umask`
22. تفاوت `umask` با `chmod`
23. تفاوت `umask` با `chown`
24. تفاوت `umask` با `setfacl`
25. مفهوم `umask` برای `user/group/others`
26. `umask`های رایج مثل `022`، `027`، `077`، `002`
27. نکات امنیتی `umask`
