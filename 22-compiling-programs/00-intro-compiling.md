# lesson 22 section 00 Introduction to compiling programs
#  بریم یه مقدمه ای بخونیم از کامپایل شدن و پیش نیازها برای درک مفهوم کامپایل
#ا <mark> این فصل رو میزارم برایه مطالبه حفظی که یه اشناییتی داشته باشید با این مفاهیم</mark>
---
## اول <mark>Source code </mark>یعنی چی ؟
سورس کد یا کد منبع ، یعنی اون متن دستوری که  اون برنامه نویس مینویسه تا یک برنامه ساخته شه.
مثلا این یک سورس کد یا کد منبع خیلی ساده 😡است که به زبون c نوشته شده:
```
#include <stdio.h>

int main() {
    printf("Hello, World!\n");
    return 0;
}
```
که به طور مثال اون برنامه نویس میاد این محتوا رو درون فایلی به اسمه hello.c میگذارد🙄:

حالا مشکل چیه🤔؟مشکل اینه که انسان میتونه این کد رو بخونه ولی CPU نمیتونه.

## حالا <mark>compile</mark> یعنی چی؟
حالا کامپایل کردن /compile یعنی تبدیل کد منبع یا سورس کد  به برنامه ای که سیستم عامل و پردازنده بتوانند اون رو اجرا کنند.

## حالا <mark>compiler</mark> چیکار میکند؟
کامپایلر همون مترجمی است که میاد کد منبع یا سورس کد رو به زبان قابل فهم برایه ماشین درمیاره چند نمونه از کامپایلر ها :
- gcc --->C language
- ++g-->compiler C++

---
## (1) ما چجوری برنامه هارو دانلود میکنیم؟

الان اگر دقت بکنید ما برنامه هایی که دانلود میکنیم رو به این صورت دانلود میکنیم:
- 1 اپدیت ریپازیتوری که در این ریپازینتوری ها برنامه هایه از پیش کامپایل شده قرار دارد که ما این اپدیت ریپازیتوری رو با دستور-----> sudo apt update زده و لیستشو رفرش میکنیم.


- 2 حالا هم خیلی راحت با ازار مدیریت بسته که در توزیع های مختلف مختلف است اون رو نصب میکنیم به این صورت -------->sudo apt install nameprogram

## (2) حالا یه داستان پیش میاد ایا رویه همه توزیع ها مثل دبیان/ابونتو رویه بروز بودن اون برنامه ها تاکید دارند یا خیر؟
اینجا همون جایی که میفهمید کامپایل به چه درد میخوره ببینید در حات عادی در اون توزیع هایی که خیلی محبوبه و پراستفاده مثل  دبیان سریع برنامه هارو بروز میکنن و یه صورت اماده درون رپو قرار میدن که شما به راحتی با دونستن اسم اون برنامه با دو مرحله بالا میتونید اون رو استفاده:

- باز ولی شما شاید یه برنامه ای رو بخواید که درون ریپازیتوری نباشه یا اخرین نسخه نباشه یا ....
- 
- اینجاست که <mark>compile</mark> میاد وسط  و شما بهش نیاز پیدا میکنید


## (3) دو دلیل مهم برایه کامپایل کردن شد؟
- 1در دسترس نبودن اون  unavailability بودن اون 
- 2 جدید بودن و به موقع بودن  Timeline

# یدونه ریپازیتوری درمورد زبون C میخوام بسازم که درون اون بیشتر توضیح میدم و فنی تر فقط این سوالات و جواب هارو بدونید که کسی ازتون پرسید گیر نکنید توش-->مهندسی گفتن🙄😂

## 
