# ا-Mount Point چیست؟

ا-**Mount Point یک Directory معمولی است که به عنوان محل اتصال File System استفاده می‌شود.**

مثلاً:

```
/data
```

یک Mount Point است.

قبل از Mount:

```
/data

(یک پوشه خالی معمولی)
```

بعد از Mount:

```
/data

(محتویات Disk جدید)
```